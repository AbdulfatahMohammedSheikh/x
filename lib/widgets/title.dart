import 'package:flutter/material.dart';

class Title extends StatelessWidget {
    //TODO: using this widget instead of the rendom text widget. 
    //TODO: create a class for requler text like the body and the descriptoin.
  const Title({super.key});

  @override
  Widget build(BuildContext context) {
      return Text(""); 
  }
  
}
